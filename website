import React, { useState, useEffect } from 'react';
import { Heart, Book, Users, Sparkles, MessageCircle, TrendingUp, Calendar, Target, Compass, Lightbulb, Star, ChevronRight, Play, Pause } from 'lucide-react';

export default function VeritasDashboard() {
  const [currentTime, setCurrentTime] = useState(new Date());
  const [activeTab, setActiveTab] = useState('journal');
  const [isAnimating, setIsAnimating] = useState(true);

  useEffect(() => {
    const timer = setInterval(() => setCurrentTime(new Date()), 1000);
    return () => clearInterval(timer);
  }, []);

  const journalEntries = [
    {
      date: "Today",
      title: "Finding Purpose in Daily Challenges",
      preview: "Reflected on how compassion can transform difficult conversations...",
      virtues: ["Compassion", "Patience"],
      mood: "Reflective"
    },
    {
      date: "Yesterday", 
      title: "Gratitude in Small Moments",
      preview: "Discovered meaning in simple acts of kindness today...",
      virtues: ["Gratitude", "Humility"],
      mood: "Peaceful"
    },
    {
      date: "2 days ago",
      title: "Navigating Faith Questions",
      preview: "Exploring what it means to live authentically...",
      virtues: ["Authenticity", "Courage"],
      mood: "Curious"
    }
  ];

  const insights = [
    {
      icon: Compass,
      title: "Your Moral Compass",
      content: "This week, you've shown strong alignment with compassion and justice in your daily decisions.",
      action: "Explore deeper"
    },
    {
      icon: TrendingUp,
      title: "Growth Pattern",
      content: "Your reflections show increasing clarity about your values and purpose over the past month.",
      action: "View trends"
    },
    {
      icon: Lightbulb,
      title: "Today's Wisdom",
      content: "Consider how small acts of kindness can create ripple effects in your community.",
      action: "Journal about this"
    }
  ];

  const virtueProgress = [
    { name: "Compassion", progress: 85, color: "bg-blue-500" },
    { name: "Integrity", progress: 78, color: "bg-green-500" },
    { name: "Courage", progress: 65, color: "bg-purple-500" },
    { name: "Humility", progress: 72, color: "bg-orange-500" }
  ];

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-100">
      {/* Animated Background Elements */}
      <div className="fixed inset-0 overflow-hidden pointer-events-none">
        <div className={`absolute top-20 left-20 w-72 h-72 bg-blue-200 rounded-full opacity-20 ${isAnimating ? 'animate-pulse' : ''}`}></div>
        <div className={`absolute bottom-20 right-20 w-96 h-96 bg-purple-200 rounded-full opacity-15 ${isAnimating ? 'animate-pulse' : ''}`} style={{animationDelay: '1s'}}></div>
        <div className={`absolute top-1/2 left-1/2 w-64 h-64 bg-indigo-200 rounded-full opacity-10 ${isAnimating ? 'animate-pulse' : ''}`} style={{animationDelay: '2s'}}></div>
      </div>

      {/* Header */}
      <header className="relative z-10 bg-white/80 backdrop-blur-md border-b border-gray-200 shadow-sm">
        <div className="max-w-7xl mx-auto px-6 py-4">
          <div className="flex items-center justify-between">
            <div className="flex items-center space-x-3">
              <div className="w-10 h-10 bg-gradient-to-br from-blue-600 to-purple-600 rounded-xl flex items-center justify-center">
                <Book className="w-6 h-6 text-white" />
              </div>
              <div>
                <h1 className="text-2xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
                  Veritas
                </h1>
                <p className="text-sm text-gray-600">Your spiritual director in your pocket</p>
              </div>
            </div>
            <div className="flex items-center space-x-4">
              <div className="text-right">
                <p className="text-sm text-gray-500">Good evening, Sarah</p>
                <p className="text-xs text-gray-400">{currentTime.toLocaleDateString()}</p>
              </div>
              <div className="w-10 h-10 bg-gradient-to-br from-pink-400 to-purple-500 rounded-full flex items-center justify-center">
                <Heart className="w-5 h-5 text-white" />
              </div>
            </div>
          </div>
        </div>
      </header>

      {/* Hero Section */}
      <div className="relative z-10 max-w-7xl mx-auto px-6 py-12">
        <div className="text-center mb-12">
          <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
            Discover Purpose Through
            <span className="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent"> Reflection</span>
          </h2>
          <p className="text-xl text-gray-600 max-w-3xl mx-auto mb-8">
            AI-powered spiritual guidance rooted in timeless wisdom, personalized for your unique journey of faith and self-discovery.
          </p>
          <div className="flex flex-wrap justify-center gap-6 text-sm text-gray-500">
            <div className="flex items-center space-x-2">
              <Users className="w-4 h-4" />
              <span>2.8M Gen Z Catholics</span>
            </div>
            <div className="flex items-center space-x-2">
              <Book className="w-4 h-4" />
              <span>56M Active Journalers</span>
            </div>
            <div className="flex items-center space-x-2">
              <Sparkles className="w-4 h-4" />
              <span>AI-Personalized Insights</span>
            </div>
          </div>
        </div>

        {/* Main Dashboard */}
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Left Column - Journal & Navigation */}
          <div className="lg:col-span-2 space-y-6">
            {/* Navigation Tabs */}
            <div className="bg-white/80 backdrop-blur-md rounded-2xl shadow-lg border border-gray-200 p-6">
              <div className="flex space-x-1 bg-gray-100 rounded-xl p-1">
                {[
                  { id: 'journal', label: 'Journal', icon: Book },
                  { id: 'insights', label: 'Insights', icon: Lightbulb },
                  { id: 'growth', label: 'Growth', icon: TrendingUp }
                ].map(tab => (
                  <button
                    key={tab.id}
                    onClick={() => setActiveTab(tab.id)}
                    className={`flex items-center space-x-2 px-4 py-2 rounded-lg font-medium transition-all ${
                      activeTab === tab.id 
                        ? 'bg-white text-blue-600 shadow-sm' 
                        : 'text-gray-600 hover:text-gray-900'
                    }`}
                  >
                    <tab.icon className="w-4 h-4" />
                    <span>{tab.label}</span>
                  </button>
                ))}
              </div>
            </div>

            {/* Content Area */}
            {activeTab === 'journal' && (
              <div className="space-y-4">
                {journalEntries.map((entry, index) => (
                  <div key={index} className="bg-white/80 backdrop-blur-md rounded-2xl shadow-lg border border-gray-200 p-6 hover:shadow-xl transition-all cursor-pointer group">
                    <div className="flex items-start justify-between mb-3">
                      <div>
                        <h3 className="text-lg font-semibold text-gray-900 group-hover:text-blue-600 transition-colors">
                          {entry.title}
                        </h3>
                        <p className="text-sm text-gray-500">{entry.date}</p>
                      </div>
                      <ChevronRight className="w-5 h-5 text-gray-400 group-hover:text-blue-600 transition-colors" />
                    </div>
                    <p className="text-gray-600 mb-4">{entry.preview}</p>
                    <div className="flex items-center justify-between">
                      <div className="flex flex-wrap gap-2">
                        {entry.virtues.map(virtue => (
                          <span key={virtue} className="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-xs font-medium">
                            {virtue}
                          </span>
                        ))}
                      </div>
                      <span className="text-xs text-gray-500 font-medium">{entry.mood}</span>
                    </div>
                  </div>
                ))}
              </div>
            )}

            {activeTab === 'insights' && (
              <div className="space-y-4">
                {insights.map((insight, index) => (
                  <div key={index} className="bg-white/80 backdrop-blur-md rounded-2xl shadow-lg border border-gray-200 p-6">
                    <div className="flex items-start space-x-4">
                      <div className="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-500 rounded-xl flex items-center justify-center">
                        <insight.icon className="w-6 h-6 text-white" />
                      </div>
                      <div className="flex-1">
                        <h3 className="text-lg font-semibold text-gray-900 mb-2">{insight.title}</h3>
                        <p className="text-gray-600 mb-4">{insight.content}</p>
                        <button className="text-blue-600 font-medium hover:text-blue-700 transition-colors">
                          {insight.action} →
                        </button>
                      </div>
                    </div>
                  </div>
                ))}
              </div>
            )}

            {activeTab === 'growth' && (
              <div className="bg-white/80 backdrop-blur-md rounded-2xl shadow-lg border border-gray-200 p-6">
                <h3 className="text-lg font-semibold text-gray-900 mb-6">Virtue Development</h3>
                <div className="space-y-4">
                  {virtueProgress.map((virtue, index) => (
                    <div key={index}>
                      <div className="flex justify-between items-center mb-2">
                        <span className="font-medium text-gray-700">{virtue.name}</span>
                        <span className="text-sm text-gray-500">{virtue.progress}%</span>
                      </div>
                      <div className="w-full bg-gray-200 rounded-full h-2">
                        <div 
                          className={`${virtue.color} h-2 rounded-full transition-all duration-1000`}
                          style={{ width: `${virtue.progress}%` }}
                        ></div>
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            )}
          </div>

          {/* Right Column - Quick Actions & Stats */}
          <div className="space-y-6">
            {/* Quick Actions */}
            <div className="bg-white/80 backdrop-blur-md rounded-2xl shadow-lg border border-gray-200 p-6">
              <h3 className="text-lg font-semibold text-gray-900 mb-4">Quick Actions</h3>
              <div className="space-y-3">
                <button className="w-full flex items-center justify-between p-4 bg-gradient-to-r from-blue-500 to-purple-500 text-white rounded-xl hover:from-blue-600 hover:to-purple-600 transition-all">
                  <span className="font-medium">New Journal Entry</span>
                  <MessageCircle className="w-5 h-5" />
                </button>
                <button className="w-full flex items-center justify-between p-4 bg-gray-100 text-gray-700 rounded-xl hover:bg-gray-200 transition-all">
                  <span className="font-medium">Daily Reflection</span>
                  <Target className="w-5 h-5" />
                </button>
                <button className="w-full flex items-center justify-between p-4 bg-gray-100 text-gray-700 rounded-xl hover:bg-gray-200 transition-all">
                  <span className="font-medium">Explore Virtues</span>
                  <Compass className="w-5 h-5" />
                </button>
              </div>
            </div>

            {/* Stats Card */}
            <div className="bg-white/80 backdrop-blur-md rounded-2xl shadow-lg border border-gray-200 p-6">
              <h3 className="text-lg font-semibold text-gray-900 mb-4">Your Journey</h3>
              <div className="space-y-4">
                <div className="flex items-center justify-between">
                  <span className="text-gray-600">Journal Entries</span>
                  <span className="text-2xl font-bold text-blue-600">127</span>
                </div>
                <div className="flex items-center justify-between">
                  <span className="text-gray-600">Days Active</span>
                  <span className="text-2xl font-bold text-green-600">89</span>
                </div>
                <div className="flex items-center justify-between">
                  <span className="text-gray-600">Insights Gained</span>
                  <span className="text-2xl font-bold text-purple-600">45</span>
                </div>
              </div>
            </div>

            {/* Testimonial */}
            <div className="bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl shadow-lg p-6 text-white">
              <div className="flex items-center mb-4">
                {[...Array(5)].map((_, i) => (
                  <Star key={i} className="w-4 h-4 text-yellow-300 fill-current" />
                ))}
              </div>
              <blockquote className="text-sm mb-4 italic">
                "A lot of the students I speak with are spiritually starving yet the problem of getting someone to take that first step remains a hurdle"
              </blockquote>
              <cite className="text-xs opacity-90">— Rev. Drew Clary, C.S.C., Holy Cross</cite>
            </div>
          </div>
        </div>

        {/* Bottom CTA Section */}
        <div className="mt-16 text-center">
          <div className="bg-white/80 backdrop-blur-md rounded-2xl shadow-lg border border-gray-200 p-8">
            <h3 className="text-2xl font-bold text-gray-900 mb-4">
              Ready to Transform Your Spiritual Journey?
            </h3>
            <p className="text-gray-600 mb-6 max-w-2xl mx-auto">
              Join millions discovering purpose, meaning, and moral clarity through AI-powered spiritual direction rooted in timeless wisdom.
            </p>
            <div className="flex flex-wrap justify-center gap-4">
              <button className="px-8 py-3 bg-gradient-to-r from-blue-500 to-purple-500 text-white rounded-xl font-medium hover:from-blue-600 hover:to-purple-600 transition-all shadow-lg">
                Start Your Journey
              </button>
              <button className="px-8 py-3 bg-gray-100 text-gray-700 rounded-xl font-medium hover:bg-gray-200 transition-all">
                Learn More
              </button>
            </div>
          </div>
        </div>
      </div>

      {/* Animation Control */}
      <button
        onClick={() => setIsAnimating(!isAnimating)}
        className="fixed bottom-6 right-6 w-12 h-12 bg-white/80 backdrop-blur-md rounded-full shadow-lg border border-gray-200 flex items-center justify-center hover:bg-white transition-all"
      >
        {isAnimating ? <Pause className="w-5 h-5 text-gray-600" /> : <Play className="w-5 h-5 text-gray-600" />}
      </button>
    </div>
  );
}
